# 環境變數設定指南

## 必要環境變數

本專案需要以下環境變數才能正常運作。請在專案根目錄建立 `.env` 檔案，並填入對應的值。

### 1. POSTGRES_URL

PostgreSQL 資料庫連線字串。

**格式**:
```
postgresql://[使用者]:[密碼]@[主機]:[埠口]/[資料庫名稱]
```

**範例**:
```env
POSTGRES_URL=postgresql://myuser:mypassword@localhost:5432/bookkeeping
```

**取得方式**:

#### Vercel Postgres (推薦)
1. 登入 [Vercel Dashboard](https://vercel.com/dashboard)
2. 前往 Storage 頁面
3. 建立新的 Postgres 資料庫
4. 複製 `POSTGRES_URL` 連線字串

#### 本地 PostgreSQL
1. 安裝 PostgreSQL
2. 建立資料庫: `CREATE DATABASE bookkeeping;`
3. 建立使用者並授權
4. 使用上述格式組合連線字串

#### 其他託管服務
- [Supabase](https://supabase.com/)
- [Neon](https://neon.tech/)
- [Railway](https://railway.app/)
- [Heroku Postgres](https://www.heroku.com/postgres)

### 2. GEMINI_API_KEY

Google Gemini AI API 金鑰。

**範例**:
```env
GEMINI_API_KEY=AIzaSyAbCdEfGhIjKlMnOpQrStUvWxYz1234567
```

**取得方式**:
1. 前往 [Google AI Studio](https://ai.google.dev/)
2. 登入 Google 帳號
3. 點擊「Get API Key」
4. 建立新的 API Key (或使用現有的)
5. 複製 API Key

**注意事項**:
- 免費方案有使用額度限制
- 建議設定使用配額避免超額
- 不要將 API Key 公開或上傳到版本控制

### 3. AUTH_SECRET

用於加密 Session 的隨機字串 (至少 32 字元)。

**範例**:
```env
AUTH_SECRET=your_super_secret_random_string_here_32_chars_minimum
```

**生成方式**:

#### 方法 1: OpenSSL (推薦)
```bash
openssl rand -base64 32
```

#### 方法 2: Node.js
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

#### 方法 3: 線上工具
- [RandomKeygen](https://randomkeygen.com/)
- [1Password Generator](https://1password.com/password-generator/)

**注意事項**:
- 請使用真正的隨機字串
- 不要使用示例值
- `AUTH_SECRET` 與一般第三方 API Key/Secret 不同，通常不建議在生產環境中例行定期輪替
- 生產環境一旦設定後應避免任意更改，因為變更後通常會導致所有 Session 失效
- 若因資安事件或政策要求必須輪替 `AUTH_SECRET`，請同步規劃完整的 session/token 失效策略與使用者重新登入流程

### 4. NEXTAUTH_URL

應用程式的完整網址 (包含協議)。

**本地開發**:
```env
NEXTAUTH_URL=http://localhost:3000
```

**生產環境範例**:
```env
NEXTAUTH_URL=https://yourdomain.com
```

**注意事項**:
- 本地開發可省略 (預設為 `http://localhost:3000`)
- 生產環境必須設定
- 必須包含協議 (http:// 或 https://)
- 不要包含結尾斜線

## .env 檔案範例

```env
# 資料庫連線 (Vercel Postgres)
POSTGRES_URL=postgresql://username:password@host.region.postgres.vercel-storage.com:5432/verceldb

# Google Gemini API Key
GEMINI_API_KEY=AIzaSyAbCdEfGhIjKlMnOpQrStUvWxYz1234567

# Auth.js Secret (32+ 字元隨機字串)
AUTH_SECRET=your_super_secret_random_string_here_32_chars_minimum

# Next Auth URL (生產環境)
NEXTAUTH_URL=https://yourdomain.com
```

## 環境變數安全性

### ✅ 應該做的:
- 使用 `.env` 檔案存放敏感資訊
- 確保 `.env` 已加入 `.gitignore`
- 定期更換 API Key 和 Secret
- 使用不同的環境變數於開發/測試/生產環境
- 使用環境變數管理工具 (如 Vercel Environment Variables)

### ❌ 不應該做的:
- 將 `.env` 檔案上傳到 Git
- 在程式碼中寫死敏感資訊
- 在公開平台分享 API Key
- 使用簡單的字串作為 AUTH_SECRET
- 在前端程式碼中使用這些環境變數

## Vercel 部署設定

在 Vercel 專案設定中新增環境變數:

1. 前往 Vercel 專案頁面
2. 點擊「Settings」
3. 點擊「Environment Variables」
4. 逐一新增以下變數:
   - Name: `POSTGRES_URL`, Value: `你的資料庫連線字串`
   - Name: `GEMINI_API_KEY`, Value: `你的 API Key`
   - Name: `AUTH_SECRET`, Value: `你的隨機字串`
   - Name: `NEXTAUTH_URL`, Value: `https://你的網域.vercel.app`
5. 選擇環境: `Production`, `Preview`, `Development` (依需求勾選)
6. 儲存後重新部署

## 疑難排解

### 資料庫連線失敗
- 檢查 `POSTGRES_URL` 格式是否正確
- 確認資料庫主機可連線
- 檢查防火牆設定
- 確認資料庫帳號密碼正確

### Gemini API 錯誤
- 檢查 API Key 是否正確
- 確認 API Key 有啟用
- 檢查使用配額是否已達上限
- 確認網路可連線至 Google AI API

### Session 問題
- 檢查 `AUTH_SECRET` 是否設定
- 確認 `NEXTAUTH_URL` 是否正確
- 清除瀏覽器 Cookie 後重試
- 檢查資料庫 sessions 表是否正常

## 更多資訊

- [Nuxt 環境變數文檔](https://nuxt.com/docs/guide/directory-structure/env)
- [NuxtAuth 設定文檔](https://sidebase.io/nuxt-auth/configuration/nuxt-auth-handler)
- [Vercel 環境變數文檔](https://vercel.com/docs/projects/environment-variables)
